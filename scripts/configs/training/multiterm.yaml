_target_: dynamics_discovery.training.MultitermTrainer
optimizer:
  _target_: optax.adabelief
  learning_rate: 2e-3
gradient_strategy: config
max_epochs: 2000
#savedir: ./results/sweep/${..data.dataset.name}/downsample=${..data.downsample_factor}/len=${..data.segment_length}
savedir: ./results/smaller_datasets/${..data.dataset.name}/downsample=${..data.downsample_factor}/len=${..data.segment_length}
savename: lr=${.optimizer.learning_rate}_noise=${..data.noise_std_relative}_key=${..model.key}_${.gradient_strategy}
wandb_entity: jhko725
wandb_project: ${..data.dataset.name}_smaller_data
wandb_mode: online